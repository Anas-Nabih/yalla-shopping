import 'package:flutter/material.dart';


class StoreDDetailsView extends StatelessWidget {
  const StoreDDetailsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
